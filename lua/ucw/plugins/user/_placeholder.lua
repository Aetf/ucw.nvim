-- Keeps `import = 'ucw.plugins.user'` valid while this directory has no
-- overrides yet (lazy.nvim's `import` errors on a directory with zero .lua
-- spec files). See README.md. Delete this once a real override exists.
return {}
